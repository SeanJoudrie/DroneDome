node_modules/
dist/
# A second build, so the Playwright checks can be run against a change while
# the previous build is still being swept on another port.
dist-test/
*.local
.DS_Store

# never commit credentials
.env
.env.*
!.env.example
assets-src/
.sketchfab-token
*.tsbuildinfo

# A second build, for running the Playwright gates against the deployed base path.
dist-ci/

# Throwaway diagnostics written while chasing a bug. Anything worth keeping
# graduates to scripts/ with a comment explaining what it caught.
.w*.mjs
.lw.ts
